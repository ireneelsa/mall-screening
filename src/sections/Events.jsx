import React from 'react';
import SectionWrapper from '../components/SectionWrapper';

const Events = () => {
  return (
    <SectionWrapper id="events" title="Events">
      <p className="max-w-2xl mx-auto text-muted font-light leading-relaxed">
        Global premieres, fashion weeks, and cultural moments that captivate millions.
      </p>
    </SectionWrapper>
  );
};

export default Events;
