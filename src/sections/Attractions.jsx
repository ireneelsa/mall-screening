import React from 'react';
import SectionWrapper from '../components/SectionWrapper';

const Attractions = () => {
  return (
    <SectionWrapper id="attractions" title="Attractions">
      <p className="max-w-2xl mx-auto text-muted font-light leading-relaxed">
        Record-breaking indoor theme parks, water parks, and year-round snow slopes.
      </p>
    </SectionWrapper>
  );
};

export default Attractions;
